{% set current_path = app.request.get('_route')|split('_') %}
<nav class="navbar fixed-top navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
        <h3><a class="navbar-brand" href="#"><i class="fa-solid fa-podcast"></i> EcoTour</a></h3>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link" href="#"><i class="fa-solid fa-house"></i> Home</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#"><i class="fa-solid fa-circle-play"></i> Audioguides</a>
                </li>
            </ul>
        </div>
        {% if app.user %}
            <h5>Conectado como {{ app.user.email }}</h5>
            <a href="{{ path('app_logout_en') }}" class="btn btn-danger">Cerrar sesión</a>
        {% else %}
            <a href="{{ path('app_login_en') }}" class="btn btn-primary">Iniciar sesión</a>
        {% endif %}

    </div>
</nav>
